import 'package:flutter/material.dart';

class VersionAppInfo extends StatelessWidget {
  const VersionAppInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Versão 1.0.3");
  }
}